# 오답노트 고도화 로드맵

> 2026-07-16 설계 문서. 코드 변경 없음 — 구현 순서·스키마 방향·의사결정 기록용.
> 현재 구현(`src/lib/wrong-notes.ts`, `mypage/wrong-notes/*`) 분석을 전제로 한다.

## 0. 현재 상태 요약 (출발점)

이미 갖춰진 것:

| 자산 | 위치 | 비고 |
|---|---|---|
| 문항 단위 정오 기록 | `cbt_attempts` + `cbt_attempt_answers` | 선택지·정오·응시별 전부 보존 |
| 오답 집계 로직 | `src/lib/wrong-notes.ts` | 틀린 횟수, "극복"(최근 응시에서 맞힘) 판정까지 계산 |
| 오답노트 UI | 마이페이지 탭 → 과목 → 문제지 드릴다운 | 문항 이미지·정답·AI 해설 표시 |
| 문항별 개념 키워드 | `question_explanations.keyword_title` | 해설 배치 루틴이 자동 축적 중 — **약점 진단의 핵심 재료** |
| 단원 태그 컬럼 | `questions.unit_tag` | 스키마에 "오답노트 단원별 분석용"으로 예약만 돼 있고 미사용 |
| CBT 솔버 | `cbt-solver.tsx` 일식 | 문제지 단위 풀이 전제 |
| 시간당 제한 패턴 | `explanation_access_log` | "일 1회" 제한 구현 시 참고 가능 |

핵심 한계: 모든 것이 **"문제지" 단위**로 묶여 있다. 과목 오답노트도 문제지 카드
목록이지 문항이 평평하게 모이지 않고, CBT도 문제지 하나만 풀 수 있다. 이번
로드맵의 본질은 **오답 데이터의 기본 단위를 문제지에서 "문항"으로 내리는 것**이다.

---

## Phase 1 — 과목별 틀린문제 모아보기 (기반 겸 첫 기능)

**목표**: 과목 오답노트를 "문제지 카드 목록"에서 "그 과목에서 틀린 문항 전체를
한 화면에 펼친 목록"으로 확장. 이후 모든 기능(섞어풀기·진단)의 데이터 통로가 된다.

### 만들 것
- 과목 오답노트에 뷰 전환: 기존 "문제지별" 탭 + 신규 **"문항 모아보기"** 탭.
- 필터/정렬: 미극복만 · 2번 이상 틀림 · 최근 틀린 순 · 자주 틀린 순 · 연도/급수.
- 문항 카드는 기존 `wrong-note-question-card.tsx` 재사용(출처 문제지 표시만 추가).
- (선택) 인쇄/PDF 내보내기 — 기존 `print-button`/`explanation-auto-print` 패턴 재사용.

### 데이터 작업
- `getSubjectWrongNoteOverview`는 현재 요약만 주므로, 과목 단위로 문항
  상세(이미지·정답·해설)까지 합치는 `getSubjectWrongNoteQuestions` 계열 추가.
  `fetchQuestionMedia`/`fetchCorrectAnswers`/`fetchExplanations`가 이미
  다중 paper를 받으므로 조합만 하면 된다.
- **성능 주의**: 회독이 쌓인 사용자는 문항 수가 수백 개가 될 수 있다.
  페이지네이션 또는 "미극복 우선 + 더보기"로 시작하고, 무거워지면 그때
  집계 테이블(Phase 2의 `user_question_status`)로 옮긴다. 처음부터 캐시 테이블을
  만들지 않는다 — 파생 데이터는 파생인 채로 시작하는 게 되돌리기 쉽다.

### 완료 기준
과목 페이지에서 필터 걸고 문항만 쭉 넘겨보는 흐름이 실제 회독 10회+ 계정에서
2초 안에 뜬다.

---

## Phase 2 — 섞어풀기 (오답 재풀이 CBT)

**목표**: 선택한 오답들을 무작위로 섞어 CBT로 다시 풀고, 그 결과가 오답노트의
"극복" 상태에 반영되게 한다. 이 Phase가 스키마상 가장 중요한 결정 지점이다.

### 핵심 설계 결정: 문항 단위 상태 테이블 도입
현재 "극복"은 *같은 문제지를 통째로 재응시*했을 때만 갱신된다(파생 계산).
섞어풀기는 문제지 단위 응시가 아니므로, 파생만으로는 표현이 안 된다. 도입할 것:

```
review_sessions        -- 섞어풀기 세션 (user_id, 필터 조건 스냅샷, score, created_at)
review_session_items   -- 세션 문항 (session_id, question_id, selected_choice, is_correct)
user_question_status   -- 문항별 통합 상태 (user_id, question_id,
                       --   wrong_count, last_result, last_answered_at, source)
```

- `user_question_status`는 문제지 CBT 채점 시(`cbt-attempt.ts`)와 섞어풀기 채점 시
  양쪽에서 갱신한다. 오답노트 화면의 극복 판정을 이 테이블 기준으로 바꾸면
  "섞어풀기에서 맞히면 극복" 이 자연스럽게 성립한다.
- 기존 사용자 데이터는 `cbt_attempt_answers`에서 일회성 백필.
- **주의(RLS)**: 채점은 반드시 서버에서. `paper_answers`는 정답 유출 방지로
  일반 SELECT가 막혀 있으므로 섞어풀기 채점도 기존 CBT처럼 service role 경유.

### 만들 것
- 진입점: 과목 오답노트 상단 "섞어풀기 시작" 버튼 → 설정 시트
  (범위: 이 과목/전체, 미극복만 여부, 문항 수 10/20/전체, 순서 섞기).
- 풀이 화면: `cbt-solver`를 "가상 문제지"(문항 배열)를 받을 수 있게 일반화.
  문항마다 출처(문제지·번호)는 결과 화면에서만 노출(풀이 중엔 힌트가 되므로 숨김).
- 결과 화면: 맞힌 문항은 극복 처리 표시, 틀린 문항은 즉시 해설 노출.
- **엣지 케이스**: 이미지 미크롭 문항은 출제 대상에서 제외(`question_images` 존재
  검사), `voided_questions`(정답 없음 처리 문항) 제외, 5지선다/4지선다 혼재 시
  문항별 `choice_count` 사용(이미 문항 테이블에 있음).

### 완료 기준
오답 30개인 계정으로 "미극복만 20문항 섞어풀기" → 채점 → 오답노트로 돌아왔을 때
맞힌 문항에 극복 배지가 붙는 왕복 흐름 완주.

---

## Phase 3 — AI 약점 진단 (일 1회)

**목표**: 사용자의 오답 이력을 AI가 읽고 "무엇이 약한지 + 뭘 하면 되는지"를
하루 1회 리포트로 제공.

### 재료 (이미 있음)
- 오답 문항 × `question_explanations.keyword_title` 조인 → **틀린 개념 분포**.
  이게 이 기능의 승부처다. 해설 배치 루틴이 커버리지를 계속 넓히고 있으므로
  시간이 갈수록 진단이 정교해진다.
- 과목별 정오율·회독별 추이(`cbt_attempts.score` 시계열), 풀이 시간
  (`duration_seconds`), 반복 오답(wrong_count ≥ 2), 극복률.

### 스키마·제한
```
ai_diagnoses (user_id, diagnosis_date date, report jsonb, model, created_at,
              UNIQUE (user_id, diagnosis_date))
```
- "일 1회"는 unique 제약으로 강제 — `explanation_access_log`식 카운트보다 단순하고
  경합에도 안전. 당일 리포트가 있으면 캐시 표시, 없으면 생성 버튼 활성화.
- 리포트는 jsonb 구조화(취약 개념 top N + 근거 문항, 과목별 진단 한 줄, 추천
  액션)로 저장해 UI가 안정적으로 그리게 한다. 자유 서술 마크다운으로 받지 말 것.

### 생성 방식 결정: 온디맨드 API 호출 (권장)
| | 온디맨드 API (권장) | 야간 배치(기존 루틴식) |
|---|---|---|
| 비용 | 누른 사용자만, Haiku/Sonnet이면 회당 원 단위 | 전 활성 사용자 선생성 → 낭비 큼 |
| 신선도 | 버튼 누른 시점 데이터 | 최대 하루 지연 |
| 인프라 | 서버 액션 + API 키 1개 | 루틴 슬롯·겹침 관리 부담 추가 |

해설 배치와 달리 진단은 사용자가 요청할 때만 가치가 있으므로 온디맨드가 맞다.
입력은 원문 이미지가 아니라 **집계된 통계 + 개념 키워드 목록**(텍스트)만 넣으면
되므로 토큰이 작고, 구조화 출력(tool use)으로 받으면 파싱도 안정적이다.
단, 실비 API 과금이 새로 생기는 지점이므로 배포 전에 회당 비용 상한을 계산해 둘 것.

### 콜드 스타트 규칙
- 최소 데이터 문턱(예: 누적 오답 15개 또는 응시 3회) 미만이면 버튼 대신
  "○문제 더 풀면 진단을 받을 수 있어요" 안내 — 빈약한 데이터로 뻔한 리포트를
  주는 것이 기능 신뢰를 가장 빨리 깎는다.
- 개념 키워드가 없는 문항(해설 미생성)은 과목/문제지 수준 통계로만 반영.

### 리포트 → 행동 연결 (중요)
진단은 읽고 끝나면 안 된다. 각 취약 개념 항목에서 바로:
- "이 개념 틀린 문항 모아보기" (Phase 1 필터 딥링크)
- "이 개념만 섞어풀기" (Phase 2 프리셋 딥링크)
로 이어지게 한다. Phase 1·2가 선행돼야 하는 이유가 이것이다.

---

## UX 원칙 — 기능은 늘어도 표면은 늘리지 않는다

기능 4단계를 다 넣었을 때 사용자가 헷갈리는 것을 막는 장치. **혼란의 원인은
기능 개수가 아니라 진입점(메뉴·화면·버튼) 개수**다. 아래 원칙을 모든 Phase에
공통 적용한다.

### 1. 표면은 "오답노트" 하나
사용자에게 보이는 메뉴는 지금과 똑같이 오답노트 하나뿐이다. 모아보기·섞어풀기·
진단·복습은 별도 메뉴가 아니라 전부 그 안의 구성 요소다. 전체 화면 수 상한을
5개로 고정한다:

```
마이페이지 > 오답노트 (허브)
├─ ① 허브: "오늘 카드" + AI 진단 배너 + 과목 카드 목록  ← 기존 화면 확장
├─ ② 과목 상세: [문제지별 | 문항 모아보기] 탭 + 섞어풀기 버튼
├─ ③ 풀이 화면 (기존 CBT 솔버 재사용)
├─ ④ 풀이 결과
└─ ⑤ AI 진단 리포트
```

### 2. 기능 나열이 아니라 "다음 행동 하나"를 제시
허브 최상단에는 버튼 여러 개를 늘어놓지 않고, 상황에 맞는 **CTA 하나짜리
"오늘 카드"**만 둔다. 우선순위 규칙으로 자동 선택:

- 복습 예정 문항 있음 → "오늘 복습할 문항 12개 · 시작"
- 없고 미극복 오답 충분 → "미극복 24문항 섞어풀기"
- 진단 조건 충족 + 오늘 미수령 → "AI 약점 진단 받기"
- 아무것도 없음 → 최근 과목 이어보기

나머지 기능은 카드 아래·과목 상세에 평범하게 존재한다. 뭘 할지 고르게 하지
말고, 고른 결과를 보여준다.

### 3. 점진적 공개 — 데이터가 쌓여야 기능이 나타난다
신규 사용자에게 빈 진단·빈 섞어풀기를 보여주는 것이 혼란의 최대 원인이므로,
데이터 문턱 전에는 기능 자체를 렌더링하지 않는다 (회색 비활성 버튼도 두지 않음):

| 상태 | 보이는 것 |
|---|---|
| 오답 0 | 기존 오답노트 안내만 |
| 오답 1+ | 오답 목록 + 모아보기 탭 |
| 오답 ~10+ | 섞어풀기 버튼 등장 |
| 오답 ~15+ (or 응시 3회+) | AI 진단 배너 등장 |

처음 등장할 때 "새 기능" 배지 한 번이면 온보딩은 충분하다. 별도 튜토리얼을
만들지 않는다.

### 4. Phase 4는 새 기능이 아니라 기존 표면의 속성으로 흡수
Phase 4 항목들은 전용 화면·메뉴를 만드는 순간 "기능 6개짜리 앱"이 된다.
전부 기존 표면에 얹는다 — **새 화면 0개**가 Phase 4의 설계 제약이다:

- 복습 스케줄 → 허브 "오늘 카드"의 한 상태 + 섞어풀기 기본 프리셋
- 개념 태그 → 모아보기 탭의 필터 하나
- 전국 오답률 → 문항 카드의 배지 하나
- 문항 메모 → 문항 카드의 접힌 입력 필드
- PDF 내보내기 → 모아보기 탭의 인쇄 버튼
- D-day 플랜 → 진단 리포트의 확장 섹션

### 5. 설정을 노출하지 말고 기본값으로 흡수
섞어풀기 설정 시트(범위·문항 수·순서)는 첫 진입부터 펼치지 않는다. "미극복만 ·
20문항 · 섞기"를 기본값으로 즉시 시작하게 하고, 바꾸고 싶은 사용자만 "설정"을
연다. 진단도 옵션 없이 버튼 하나. 이름도 기능명("간격 반복", "진단")보다 행동
언어("다시 풀기", "약점 보기")를 쓴다.

---

## Phase 4 — 확장 아이디어 (우선순위 낮음, 검증 후 선별)

> 공통 제약: 위 UX 원칙 4번 — 어떤 항목도 새 화면·새 메뉴를 만들지 않고
> 기존 표면(허브 카드·모아보기 필터·문항 카드·진단 리포트)에 속성으로 얹는다.

1. **복습 스케줄(간격 반복)**: 틀린 문항을 1·3·7일 후 복습 큐에 자동 배치,
   마이페이지에 "오늘 복습할 문항 N개" 배지. `user_question_status`에
   `next_review_at`만 더하면 되므로 Phase 2 스키마 위에 얹기 쉽다. 스트릭
   (`streak.ts`)과 결합하면 재방문 동기가 강해진다.
2. **개념 태그 브라우징**: ~~keyword_title을 과목 하위 태그처럼 노출~~ —
   "행정법 > 처분성" 틀린 문항 모아보기. **2026-08-05 실측으로 keyword_title로는
   불가**(개념 수 ≈ 문항 수라 태그가 아니라 문항 제목이다). 정규화·AI 병합으로도
   안 된다 — 표기 차이로 합쳐지는 건 전체에서 83그룹뿐이었다. **`unit_tag` 백필이
   선행 조건**이고, 그건 별도 과제로 분리했다.
3. **전국 오답률 통계**: `cbt_attempt_answers`를 문항별로 집계해 "응시자 68%가
   틀린 문제" 배지, "고난도만 섞어풀기" 프리셋. 익명 집계라 개인정보 부담 없음.
   응시 데이터가 충분히 쌓인 뒤에 의미가 생긴다(표본 적으면 오해만 유발).
4. **문항 메모**: 오답 카드에 사용자 메모 1필드. 구현은 얇지만 "내 노트"라는
   소유감을 만든다.
5. **오답노트 PDF 내보내기**: 시험장 직전 복습용 인쇄물. Phase 1 화면의 인쇄
   최적화로 시작해 수요 보고 전용 생성으로.
6. **D-day 학습 플랜**: 목표 시험일 등록 → 남은 기간에 미극복 문항·취약 개념을
   주 단위로 배분. AI 진단(Phase 3)의 확장판이며 가장 마지막.

---

## 구현 순서와 의존 관계

```
Phase 1 (모아보기)  ──→  Phase 2 (섞어풀기: user_question_status 도입)
                              │
                              └──→  Phase 3 (AI 진단: 진단→모아보기/섞어풀기 딥링크)
                                        └──→ Phase 4 (복습 스케줄·태그·통계·플랜)
```

- 1 → 2 → 3 순서 고정을 권장. 진단부터 만들면 "약점을 알려줬는데 할 수 있는 게
  없는" 리포트가 된다. 모아보기·섞어풀기가 진단의 처방전 역할을 한다.
- 각 Phase는 독립 배포 가능한 크기로 자른다. 특히 Phase 2의
  `user_question_status` 도입은 마이그레이션+백필이 끼므로 별도 배포로 격리.

## 다음 작업 순서 (배포 단위 PR 분할)

각 Phase를 통째로 잡지 말고 아래 배포 조각으로 나눠 진행한다. **1번부터 순서대로.**
새 기능은 이 로드맵 브랜치가 아니라 각 조각별 새 브랜치에서 작업할 것.

| 순서 | 조각 | 스키마 변경 | 크기 | 핵심 메모 |
|---|---|---|---|---|
| 1 | 과목 오답노트 "문항 모아보기" 탭 + 필터(미극복만/2번이상/정렬) | 없음 | 소 | 읽기 전용. 기존 `fetchQuestionMedia`/`fetchCorrectAnswers`/`fetchExplanations` 조합. dedup 그룹 중복 문항 처리, 회독 많은 계정 성능 확인 |
| 2 | `user_question_status` 테이블 + `cbt_attempt_answers` 백필 + 기존 CBT 채점(`cbt-attempt.ts`) 연동 | 있음 (UI 변화 0 — 격리 배포) | 중 | 해설봇 RLS 회귀 확인 필수 |
| 3 | 섞어풀기 (설정→풀이→결과), `cbt-solver` 가상 문제지 일반화, 결과가 status 갱신 | `review_sessions`/`review_session_items` 추가 | 대 | 미크롭·voided 문항 제외, 문항별 choice_count 사용 |
| 4 | 허브 개편 — "오늘 카드"(단일 CTA) + 진단 배너 자리 | 없음 | 소 | UX 원칙 2·3 적용 |
| 5 | AI 약점 진단 (온디맨드 API, 일 1회) | `ai_diagnoses` 추가 + API 키 | 중 | 콜드스타트 문턱, 진단→모아보기/섞어풀기 딥링크 |

**다음에 착수할 것: 순서 1 (문항 모아보기 탭).** Phase 4 요소(오답률 배지·개념 태그·
메모)는 이 단계에 넣지 않는다 — 각각 별도 조각으로 뒤에 흡수.

> 참고: 설계 목업(HTML 5화면)은 세션 scratchpad에만 있어 이 세션이 끝나면 사라진다.
> 필요하면 다시 생성하거나 `docs/`에 커밋할 것. 로드맵 본문만으로 재구성 가능하게 써 뒀다.

## 결정 기록 — 섞어풀기 뽑기 기준 (2026-07-29, 구현 완료)

Phase 2의 섞어풀기는 `shuffle(candidates).slice(0, limit)` 로 균등 무작위였다. 오답이
30개일 때는 문제가 없지만, 반년 회독하면 수백~수천 개가 쌓인다. 그 상태에서 균등
추출은 "2번 틀린 문항"과 "반년 전 한 번 틀린 문항"을 같은 확률로 내보내, 정작
위험한 문항을 만날 확률을 계속 희석시킨다.

**채택하지 않은 안**: `2번 이상 틀린 것만` / `30일 이내만` 필터. 구현은 제일 얇지만
조건 밖 문항이 영구히 안 나온다 — 시험 준비에서 제일 위험한 게 "오래돼서 잊은
오답"인데 그게 통째로 사라진다.

**채택**: 층(tier)별 정원제. 층 안에서는 여전히 무작위라 같은 문항이 반복되지 않는다.

| 층 | 조건 | 가중 |
|---|---|---|
| A | `wrong_count >= 2` | 3 |
| B | 30일 이내에 틀림 | 2 |
| C | 나머지 미극복 | 1 |

20문항이면 A 10 · B 7 · C 3. 어느 층이 모자라면 그 자리는 위험한 층부터 메운다
(정원이 남아 20개를 못 채우는 일은 없다). 노출은 칩 2개 — `약한 문제 우선`(기본) /
`전체 랜덤` (UX 원칙 5: 설정을 노출하지 말고 기본값으로 흡수).

- 규칙 정본: `packages/core/src/review-pick.ts` (+ `review-pick.test.ts` 로 정원 고정)
- 엣지 함수 사본: `supabase/functions/_shared/review-pick.ts` — **한쪽만 고치면 웹과
  앱이 다른 문제를 낸다. 층 기준·가중치는 반드시 양쪽 함께 고칠 것.**
- 스키마 변경 없음. `user_question_status.wrong_count` / `last_answered_at` 재사용.
- 복습(간격 반복, `review-queue.ts`)과는 다른 기능이다. 복습은 `srs_due_at` 이 된
  문항만 연체 순으로 내며 무작위가 없다. 층 정원제는 "지금 아무거나 좀 풀고 싶다"를
  받는 자리라 무작위성을 없애지 않고 기울이기만 한다.
- 앱은 뽑기 방식 칩이 아직 없다(섞어풀기 진입이 곧 시작이라 설정 자리가 없음).
  엣지 함수 기본값이 `weighted` 라 기준 자체는 웹과 같다.

## 결정 기록 — 복습 과목 보류 (2026-07-29, 구현 완료)

"지금은 국어만 판다" 같은 시기에 다른 과목이 매일 큐에 섞여 들어오면 복습 자체를
안 하게 된다. 마이페이지 `ReviewDueCard` 안 접힌 줄에서 과목을 끄고 켠다
(새 화면 0개 — UX 원칙 4).

**저장은 "끈 과목"(`review_preferences.paused_subject_ids`) 목록이다.** 켠 과목을
담는 include 목록이 아닌 이유: 나중에 새로 공부를 시작한 과목이 기본으로 켜져 있어야
한다. include 목록이면 새 과목이 조용히 빠진 채로 남고, 사용자는 그걸 버그로 읽는다.

**보류 중에도 `srs_due_at` 은 건드리지 않는다.** 보류는 잠깐 안 보는 것이지 진도를
지우는 게 아니다. 대신 다시 켤 때 밀린(연체) 문항을 며칠에 걸쳐 다시 뿌린다
(`packages/core/src/review-resume.ts`, 하루 몫 10 = 큐 상한의 절반, 최대 30일).
안 하면 재개 첫날 예보 오늘 칸이 수백으로 뜨고(사용자는 고장으로 읽는다), 큐
우선순위가 연체순이라 그 과목이 하루 상한을 통째로 먹어 다른 과목이 몇 주째 안 나온다.
문항이 아주 많으면 하루 몫을 늘려서라도 30일 안에 다 밀어 넣는다 — 마지막 날에
몰아넣으면 같은 문제가 재현된다.

- 마지막 한 과목은 못 끄게 막는다(전부 끄면 복습이 통째로 멈춘다).
- 앱은 설정 UI 없이 결과만 존중한다 — 리마인더 문구의 오답 수에서 보류 과목을 뺀다
  (`apps/mobile/app/settings.tsx`). 보류 해제 쓰기는 웹 서버 경로에만 둔다
  (`user_question_status` 는 쓰기 정책이 없어 service_role 경유여야 한다).
- **적용 범위는 복습(간격 반복)까지다.** 섞어풀기는 사용자가 그 자리에서 과목을
  직접 고르는 기능이라 보류를 적용하지 않는다.
- `subject_bookmarks`(즐겨찾는 과목)를 재사용하지 않았다. 그쪽은 과목 탐색용
  바로가기라, 즐겨찾기를 지웠다고 복습이 멈추면 안 된다.

## 결정 기록 — 홈 복습 유도 모달 (2026-07-29, 구현 완료)

로그인 사용자가 홈에 들어오면 "새 문제보다 복습 먼저"를 하루 한 번 제안한다
(`review-nudge-modal.tsx`).

- **홈 서버 렌더에 복습 요약을 넣지 않았다.** 요약 계산은 이미지 조회까지 도는 무거운
  작업인데 홈은 모두가 매번 여는 화면이다. 모달이 마운트된 뒤 로컬 저장소로 "오늘
  이미 봤는지"를 먼저 보고, 안 봤을 때만 서버 액션을 부른다 — 같은 날 두 번째
  방문부터는 네트워크 비용이 0이다.
- 하루 경계는 복습 스케줄과 같은 KST 04:00(`srsDayIndex` 재사용). 자정 기준이면
  새벽 3시에 푼 사람에게 두 시간 뒤 같은 모달이 다시 뜬다.
- 띄우는 순간 "봤음"으로 기록한다. 닫기를 안 누르고 나가도 같은 날 다시 뜨면 안 된다.
- 무료 사용자에겐 뜨지 않는다(서버 액션이 0을 돌려준다). 못 누르는 걸 모달로 띄우면
  홈 자체를 피하게 된다 — `review-due-card.tsx` 가 홈에 잠긴 카드를 두지 않은 이유와 같다.
- 오늘 복습할 문항이 0이면 뜨지 않는다.

**(2026-08-21 갱신)** 홈에 뜰 수 있는 안내 셋(개발 중 안내·이 복습 유도·출석 이벤트
광고)을 한 판에 담아 좌우로 넘기는 슬라이드로 바꾸면서, 이 모달은 그 한 장이 됐다
(`review-nudge-slide.tsx`, 판은 `home-popup-slider.tsx`). 판정·조회 방식은 그대로다.
바뀐 것은 "봤음" 기록 시점 하나 — **띄우는 순간 → 그 장이 실제로 눈앞에 온 순간**으로
옮겼다. 예전에는 셋 중 하나만 뜨고 나머지는 물러났는데(그때도 물러난 쪽은 기록하지
않았다), 지금은 뒷장으로 함께 실리므로 "실려는 있었지만 넘겨 보지 않은" 상태가
생긴다. 그건 본 게 아니므로 기록하지 않고, 같은 날 다음 방문에 다시 기회를 얻는다.

## 결정 기록 — 복습 알고리즘 재검토 (2026-08-04, 구현 완료)

간격 반복을 얹은 지 며칠 뒤 공시생 관점에서 다시 본 결과. 골격(대기 풀·하루 상한·
leech·KST 04시 경계)은 그대로 두고 네 가지를 고쳤다. **D-day(목표 시험일) 연동은
별도 작업으로 뺐다.**

### 1. 경과일을 보지 않던 스케줄 갱신 (가장 컸던 문제)

`recordQuestionResults`는 채점된 모든 문항에 `nextSrs`를 돌리는데, `nextSrs`는
"같은 날 + 정답" 말고는 시점을 보지 않았다. 그런데 이 서비스의 채점은 복습
세션에서만 일어나지 않는다 — **공시생의 기본 학습 방식이 문제지 회독**이고,
섞어풀기도 스케줄을 무시한다. 그래서 이런 일이 정상적으로 벌어졌다:

| 상황 | 예전 | 지금 |
|---|---|---|
| 간격 62일 문항을 5일 만에 회독에서 맞힘 | 62 → 174일 | 5일치만 인정(≈69일) |
| 간격 62일 문항을 5일 만에 회독에서 틀림 | 1일 리셋 + lapse + leech 진행 | 반감(31일), lapse 없음 |

두 축으로 나눠 판단한다. **오답은 "예정일이 됐나"**(`dueProgress`) — 예정일 전에
끌려 나와 틀린 건 스케줄의 실패가 아니다. **정답은 "실제로 얼마나 버텼나"**
(`retainedProgress`) — 62일 뒤로 잡아둔 걸 5일 만에 맞힌 건 5일치 기억이다.
정답 배수는 `1 + (ease − 1) × 비율`이라 비율 1이면 예전과 똑같은 × ease다.

조기 오답의 재확인(3시간 뒤)에서 또 틀리면 그때는 예정일이 지난 뒤라 정상
lapse로 처리된다 — 완화가 leech 판정을 무력화하지 않는다.

`user_question_status.srs_due_at`을 `nextSrs`에 넘겨주기만 하면 되고, 스키마 변경은
없다. dueAt을 안 주면 예전 동작 그대로라 마이그레이션 순서도 안 탄다.

### 2. 모든 정답을 Anki의 "쉬움"으로 취급하던 ease

`SRS_EASE_BONUS 0.1` / `SRS_MAX_EASE 2.8`은 SM-2에서 q=5(완벽하게 즉시 기억)일 때의
값인데, 우리 채점은 맞다/틀리다 2단계라 "겨우 맞힌 것"과 "확실히 아는 것"을 구분할
수 없다. 세 번만 맞히면 상한에 붙어 간격이 1 → 3 → 8 → 22 → 62 → 174로 뛰었다.
4지선다는 몰라도 25%, 두 개까지 좁히면 50%가 맞는다.

→ **보너스 0.05 / 상한 2.5(= 초기값)**. 정답으로는 기본값보다 후해지지 않고,
무너져 깎인 ease의 회복 전용으로만 오른다. 궤적은 1 → 3 → 8 → 20 → 50 → 125.

### 3. 큐 구성이 아니라 순서만 섞고 있던 과목 인터리빙

`interleaveBySubject`는 **뽑은 것의 순서만** 바꾼다. 행정법 due가 200개면 점수순
상위 20개가 전부 행정법이고 인터리빙은 아무 일도 못 한다. 공시는 5과목을 같은 날
보므로 한 과목 독점은 그 자체로 사고다(과목 보류 기능도 절반은 이 증상의 우회로였다).

→ 과목마다 최소 몫(`SUBJECT_MIN_SLOTS = 2`, 하루 총량에 비례)을 먼저 떼고 남은
자리는 예전처럼 위험도 경쟁. 균등 배분이 아니다 — 밀린 과목이 더 많이 나오는 건
맞는 동작이고, 막으려는 건 "국어가 며칠째 안 나옴"뿐이다.

신규 승격은 아예 과목 라운드로빈으로 간다. 승격 순서가 "자주 틀린 것 먼저"인데
1회독 중에는 전부 `wrong_count 1`로 동점이라, 그대로 두면 가장 오래전에 푼
시험지부터 순서대로 = 신규 10개가 통째로 한 과목이 된다.

### 4. 간격 fuzz

간격이 결정적이라 하루에 푼 20문항이 영원히 같은 날 함께 움직였다. 예보표의
"오늘 0, 3일 뒤 40" 같은 요철이 그 결과이고, 밀린 복습 정리·보류 해제 재분산 같은
사후 보정이 필요했던 원인 중 하나다. → 간격 4일 이상이면 ±10%(Anki와 같은 자리).

주입식(`opts.fuzz`)이라 기본은 결정적이다. `buildDueQueue`는 여전히 순수·결정적이라
"배너 숫자 = 세션 문항"이 유지된다 — 흔드는 건 쓰기 시점의 `nextSrs`뿐이다.

### 5. `srs_reviews` — 알고리즘을 검증할 수 있게 하는 로그

`user_question_status`는 "지금 상태"만 들고 있어서 **"간격 8일에서 실제 정답률이
몇 %였나"를 셀 수 없었다.** 그러면 위에서 고른 상수들(ease 2.5, 1·3일 학습 단계,
연체 점수 상한 14일, leech 8)이 맞는지 확인할 방법이 영원히 없다. 간격 반복은
실측으로 상수를 조정해야 쓸 만해지는 알고리즘이다.

채점 한 번당 한 행(append-only). 채점 직전 상태 + 실제 경과일 + 배정된 새 간격.
스케줄이 있는 문항만 남긴다(대기 풀 오답은 아직 복습이 아니라 통계에 섞이면 안 된다).
쓰기 정책 없음 — 이 로그가 상수 조정의 근거라 클라이언트가 넣을 수 있으면 오염된다.
집계 쿼리 예시는 마이그레이션 파일 하단에 있다.

- 규칙 정본: `packages/core/src/srs.ts`, `packages/core/src/review-queue.ts`
- 엣지 사본: `supabase/functions/_shared/srs.ts` — **간격·ease·조기 판정 상수는 반드시
  양쪽 함께 고칠 것.** 무작위 2만 건 대조로 파리티를 확인했다.
- 복습 큐(`buildDueQueue`)는 웹 전용이라 엣지 사본이 없다. 앱의 복습 진입은
  섞어풀기(`review-create`)라 `review-pick.ts`만 공유한다.
- 마이그레이션: `apps/web/scripts/sql/2026-08-04-srs-reviews.sql` (추가만 — 배포와
  순서를 가리지 않지만, 없는 동안의 채점은 기록이 안 남는다).

### 남겨둔 것 (다음 후보)

1. **D-day 연동** — 별도 작업 예정. 간격 상한을 남은 일수에 묶어 모든 문항이 시험
   전 최소 1회 재노출되게 하고, D-14부터 신규 승격을 0으로. `SRS_MAX_INTERVAL_DAYS
   = 180`의 주석이 "시험일이 정해져 있어"인데 정작 시험일을 모르는 상태다.
2. ~~문항별 소요시간 기록~~ — **수집하지 않기로 결정**(아래 결정 기록 참조).
   "찍었어요"가 결과 화면 수동 탭뿐이고 CBT 경로엔 그 UI가 아예 없다는 문제는
   그대로 남아 있다.
3. ~~**개념 기반 대체 출제**~~ — **2026-08-05 실측으로 무효**(아래 "`keyword_title`은
   개념 축이 아니다" 참조). 같은 개념의 다른 기출이 존재하지 않아 대체할 문항이 없다.
   `unit_tag`를 채운 뒤에야 가능해진다. 원래 취지(같은 이미지를 반복하면 선지 위치와
   정답 번호를 외운다 — 기출 SRS의 고질병, 이미지 기반이라 선지 셔플도 불가)는 유효.
4. **`duePriorityScore`의 lapses 상한** — `lapses × 3`에 상한이 없어 복구된 leech
   (lapses ≥ 8 → 24점)가 어떤 연체 문항보다도 영구히 우선이다. `min(lapses, 5)`.

## 결정 기록 — 신규 유저 시뮬레이션과 그 결론 (2026-08-04)

실제 `buildDueQueue`/`nextSrs`를 그대로 굴려 신규 유저 45일치를 돌렸다. 전제:
1일차 200문항 응시·오답 60(14/30/16), 2~14일차 매일 새 오답 40, 15일차부터 회독 종료.
복습 정답률은 처음 55%에서 통과할수록 +8%p. 총 유입 580개.

### 확인된 것 (설계대로 작동)

- 과목 최소 정원이 실제로 먹힌다. 과목2 유입이 2배인데도 과목1·3이 매일 3~8개씩
  꾸준히 나온다. 정원이 없었으면 과목2가 큐를 계속 먹었을 자리다.
- 연체가 45일 내내 13~25로 안정적이다. "오늘 20개, 끝" 감각이 유지된다.
- 예보표가 평탄하다(`20 20 15 12 12 11 12`). fuzz 효과.

### 드러난 것 — 대기 풀이 사실상 안 줄어든다

회독이 끝나 유입이 0인데도 대기 풀은 30일 동안 531 → 470, **하루 2개꼴**로만 준다.
이 속도면 470개 소화에 8개월이다. 복습 몫이 상한을 먹어 신규 몫이 자주 0이 되기 때문.

**채택하지 않은 안: 신규 몫 바닥 보장** (하루 20 중 5개는 무조건 신규).
시뮬레이션으로 반증했다 — 대기 풀은 확실히 비우지만(120일에 0) **연체가 456개로
폭발한다.** 하루 상한이 애초에 막으려던 실패 모드가 반대쪽에서 재현될 뿐이다.

| 일차 | 현재(남는 자리에만 신규) | 신규 바닥 5 | 현재 연체 | 바닥안 연체 |
|---|---|---|---|---|
| 45 | 470 | 355 | 16 | 177 |
| 120 | 359 | 0 | 22 | **456** |

**결론: 현재 설계가 옳다.** 대기 풀 정체는 버그가 아니라 용량 문제다. 하루 40개씩
새로 틀리는 속도로 회독하면서 하루 상한 20은 산술적으로 불가능한 조합이다.
45일 뒤 상태 비교(상한 60·정답률 70%면 예약 417 · 대기 161 · 연체 56로 건강해진다):

| 설정 | 예약중 | 대기풀 | 접힘 | 간격 8일+ |
|---|---|---|---|---|
| 상한 20 · 55% | 99 | 470 | 11 | 69 |
| 상한 40 · 70% | 273 | 305 | 2 | 210 |
| 상한 60 · 70% | 417 | 161 | 2 | 328 |

→ **해야 할 것은 알고리즘 수정이 아니라 안내다.** 대기 풀이 임계를 넘으면 하루
상한 상향을 권하고, 유입이 승격 능력을 크게 앞서면 ~~"새 문제지보다 오답 먼저"를
알린다~~(홈 유도 모달 자리 재활용 — 새 화면 0개). 아직 미구현.

> **2026-08-05 수정**: 뒷부분의 방향이 반대였다. 유입이 처리를 크게 앞서는
> 사용자(확장기)에게 필요한 건 "오답 먼저"가 아니라 **"진도 먼저, 오답은 맛보기"**다
> — 바로 이 시뮬레이션이 보여준 대로 하루 40개씩 틀리는 속도에서 상한 20으로
> 오답을 따라잡는 건 산술적으로 불가능하기 때문이다. "국면(확장기/정착기) 이원화"
> 결정 기록 참조. 앞부분(상한 상향 권유)은 그대로 유효하다.

부수 발견: 정답률 55%에서 상한을 40으로 올리면 접힘이 11 → 24로 는다. 자주 만나니
자주 틀려 lapses가 빨리 쌓인다. leech 임계 8이 정답률 가정에 민감하다는 뜻이라,
`srs_reviews`가 쌓이면 실측으로 정할 것.

## 결정 기록 — 전국 정답률 기반 문항 선정 (2026-08-04, 설계만·미구현)

"정답률이 아주 높은 문항은 실수에 가까우니 빼고, 아주 낮은 문항은 지엽적이니
우선순위를 낮추자"는 방향. **지금은 구현하지 않는다** — 실사용자가 없어 표본이 0이고,
필요한 재료가 전부 원본에 남아 있어 나중에 소급 계산되기 때문이다.

| 필요한 것 | 저장됨 | 소급 |
|---|---|---|
| 첫 응시만 골라내기 | `cbt_attempts.created_at` + user_id + paper_id | ✅ |
| 미응답 제외 | `cbt_attempt_answers.selected_choice` (null 보존) | ✅ |
| 찍기 응시 제외 | `cbt_attempts.duration_seconds` | ✅ |

### 지금 집계(`paper_question_wrong_rates`)가 가진 편향 — 쓰기 전에 고칠 것

1. **회독 중복**: 모든 응시를 센다. 한 사람이 10회독하면 혼자 10표고, 2·3회차는
   정답률이 훨씬 높다. 결과적으로 "문항 난이도"가 아니라 "그 문제지를 여러 번 푼
   사람이 얼마나 많은가"를 측정한다. → **사용자별 그 문제지 최초 응시 1건만.**
2. **미응답이 오답으로 집계됨**: 채점 루프가 전 문항에 행을 만들어(`papers/actions.ts`)
   안 푼 문항이 `is_correct=false`로 들어간다. 시간이 모자라 뒤쪽을 못 푼 응시가
   **뒷번호 문항의 정답률을 체계적으로 깎는다.** → 분모에서 `selected_choice is null` 제외.
3. **3분이면 제출 가능**(`MIN_ATTEMPT_SECONDS = 180`): 찍고 넘긴 응시가 섞인다.
   → 난이도 집계에만 별도의 소요시간 하한.

### 임계값은 절대 %가 아니라 "과목별 분위수 + 절대 가드레일(AND)"

앱에서 측정될 정답률은 실제 시험 정답률과 다르다. 사용자 구성(1회독 초보 vs 시험
직전 고수)에 따라 분포 전체가 통째로 이동한다. `90%` 같은 절대값을 박아두면 실제
평균이 55%로 나올 때 걸리는 문항이 0개가 되고, 85%로 나오면 절반이 감점된다.

```
과목별로 계산한다 (영어와 한국사는 기저 정답률이 다르다).

0.6  ← (그 과목 상위 20% 쉬움)   AND  (정답률 ≥ 85%)
1.25 ← 나머지 중간 구간 = 변별 구간
0.7  ← (그 과목 하위 10% 어려움) AND  (정답률 ≤ 30%)
1.0  ← 평활 후 표본 신뢰도 부족 (판단 보류)

예외: 본인이 2번 이상 틀린 문항은 감점하지 않는다 → max(1, w)
      "쉬운데 반복해서 틀린다"는 실수가 아니라 구멍이고, 오히려 제일 급하다.
```

이 형태는 **분포가 촘촘하면 자동으로 아무 일도 안 한다.** 가정이 틀렸을 때의 실패
모드가 "과잉 개입"이 아니라 "무개입"이라, 가정으로 시작할 때 이게 제일 중요하다.

표본 처리는 하드 컷 대신 평활: `p̂ = (정답수 + k·μ) / (응답수 + k)`, k ≈ 20.
9건과 10건 사이의 절벽이 없어진다.

### 적용 위치

| 자리 | 적용 | 이유 |
|---|---|---|
| 신규 승격 순서(`byPendingPriority`) | ✅ 최대 효과 | 대기 풀이 몇 달치라 "무엇을 먼저 태우나"가 사실상 "무엇을 공부하나"를 결정한다 |
| 섞어풀기 층 정원제(`review-pick.ts`) | ✅ | 층 안의 뽑기 확률에 계수를 곱한다 |
| **복습 큐 due 우선순위(`duePriorityScore`)** | ❌ **넣지 말 것** | 이미 스케줄이 잡힌 문항은 예정일에 봐야 한다. 난이도로 미루면 간격 반복이 무의미해진다 |

- **배제가 아니라 가중이다.** "90%가 맞히는 문제를 내가 틀렸다"는 그 자체로 강한
  신호(기본기 구멍)고, 배제하면 그 문항이 대기 풀에 영원히 남아 화면의 대기 숫자가
  안 줄어든다. 섞어풀기 층 정원제와 같은 이유다("조건 밖 문항은 영구히 안 나온다").
- 집계는 매번 RPC로 돌리기엔 무겁다. `question_difficulty`(paper_id, question_number,
  first_attempts, first_correct, updated_at) 테이블을 야간 배치로 갱신하는 형태가 맞다.
- `difficulty_ratings`는 **문제지 단위 별점**이라 문항 선정에 쓸 수 없다.

**착수 조건**: 첫 응시 기준 표본 30건 이상인 문항이 의미 있는 수로 쌓였을 때.
그때 정답률 분포를 먼저 재고 위 임계를 확정할 것.

## 결정 기록 — 문항별 소요시간은 수집하지 않는다 (2026-08-04)

찍기 자동 판정("정답인데 평소보다 오래 걸림 → ease 보너스 0")과 콜드스타트 난이도
신호로 검토했으나 **수집하지 않기로 했다.**

검토 내용은 남겨 둔다. 나중에 다시 필요해지면 이 설계로 시작하면 된다:

- **절대 시간은 쓸 수 없다.** 영어 독해 3분과 한국사 단답 15초가 둘 다 정상이다.
  판정은 항상 배수로 하고, baseline은 3단계 폴백:
  ① 그 문항의 중앙값(표본 ≥ 20) → ② (과목 × `questions.passage_id` 유무) 중앙값
  → ③ 같은 세션·같은 과목 문항들의 중앙값(사용자 1명부터 작동. 읽기 속도와 그날
  컨디션이 세션 내 상수라 자동 소거된다).
- **"느리게 맞힌 정답"만 쓴다.** 찍은 문항은 오히려 빠르다 — 빠른 정답을 찍음으로
  처리하면 진짜 아는 사람을 벌준다. 느린 정답만 쓰면 방향이 명확하고, 오판해도
  손해가 "간격이 조금 덜 벌어짐"뿐이다.
- **계측의 함정이 기준보다 중요하다**: 문항 왕복(누적 체류로), 자리 비움(1회 체류
  3분 절단), 탭 비활성(`visibilitychange`로 정지), 전체보기(PDF) 모드는 측정 불가,
  첫 문항은 baseline에서 제외. 이거 없이 모으면 데이터가 통째로 쓰레기가 된다.

**비용**: 소요시간은 소급 계산이 안 되는 유일한 항목이다. 나중에 필요해지면 그
시점부터의 데이터만 쌓인다(전국 정답률과 달리 과거를 복원할 수 없다).

## 결정 기록 — 국면(확장기/정착기) 이원화 (2026-08-05)

간격 반복을 얹은 뒤 "복습 화면의 숫자들이 서로를 설명하지 못한다"를 파고들다가
근본 원인에 닿았다. **이 서비스는 두 종류의 사용자에게 같은 화면을 보여주고
있는데, 그 둘에게 "오답"이라는 단어의 뜻이 다르다.**

### 두 축이 사실 하나다

공시생의 편차는 크다 — 하루 2장을 푸는 사람과 10장을 푸는 사람, 1회독 30점과
95점. 그런데 이 둘은 따로 볼 필요가 없다. **하루 유입 오답 ÷ 하루 처리 가능량**
하나로 합쳐진다.

| | 하루 2장 | 하루 10장 |
|---|---|---|
| 1회독 30점 | 오답 140/일 | 오답 700/일 |
| 1회독 95점 | 오답 10/일 | 오답 50/일 |

- **확장기(유입 ≫ 처리)**: 오답 = 아직 안 배운 범위. 30점이면 70문항이 오답인데
  그건 실패가 아니라 미학습이다. 이 사람에게 "오답 복습"은 사실상 전 범위 공부다.
- **정착기(유입 ≈ 처리)**: 오답 = 진짜 약점. 95점의 5문항은 희소하고 가치가 높다.
  간격 반복이 정확히 이 사람을 위한 도구다.

**사람의 속성이 아니라 시기의 속성이다.** 한 사람이 시간축을 따라 확장기 → 정착기로
이동한다. 그래서 사용자를 분류하지 않고 국면을 감지한다.

### 확장기에 "남은 오답 470"을 보여주지 않는다

이 결정이 이번 검토의 핵심이다. 그 숫자는 확장기 사용자에게 *"너는 470번 실패했고
하루 20개씩이면 8개월 걸린다"*로 읽힌다. 어떤 문구로도 좋은 소식이 되지 않는다
(2026-08-04 시뮬레이션이 이미 "하루 40개씩 틀리는 속도로 회독하면서 하루 상한
20은 산술적으로 불가능한 조합"임을 보였다).

대신 **올라가는 숫자**를 헤드라인에 둔다:

- **극복 누계** — "지금까지 340문항 극복". 무조건 오른다. 데이터 이미 있음.
- **문제지 진도** — "행정법 기출 47장 중 12장". 공시생이 실제로 세는 단위(회독)다.

점수 추세(`SubjectStat.recentScores`)는 보조로만. 오르내림이 있어 헤드라인에는
맞지 않는다.

### 확장기의 오늘 카드는 "복습"이 아니라 "이어풀기"다

로드맵 474줄의 미구현 안내는 확장기 사용자에게 *"새 문제지보다 오답 먼저"*를
권하자는 것이었는데, **방향이 반대다.** 확장기에 필요한 건 진도지 복습이 아니다.
오답 복습은 소량만 곁들인다(하루 몫의 일부).

이 뒤집기가 설계를 오히려 단순하게 만든다 — 확장기 모드는 새 기능이 아니라
**오늘 카드의 CTA와 헤드라인 숫자가 바뀌는 것**뿐이다. 화면 수는 그대로 유지된다
(UX 원칙 1: "표면은 오답노트 하나").

| | 확장기 | 정착기 |
|---|---|---|
| 오늘 카드 CTA | 다음 시험지 이어풀기 | 오늘 복습 N문항 |
| 헤드라인 | 극복 누계 · 문제지 진도 | 남은 오답 / 극복 |
| 간격 반복 | 곁들이기(소량) | 주력 |
| 성공 감각 | "한 장 끝냈다" | "오늘치 끝냈다" |
| 지표 | 7일 재방문율 | 오늘치 완주율 |

### 국면 감지 규칙

1. **묻지 않는다.** 첫 진입에 "어떤 유형이세요?"는 최악이다 — 본인도 모른다.
2. **최근 7일 유입 ÷ 하루 정원**으로 판정. 점수도 분량도 아닌 이 비율 하나.
3. **히스테리시스 필수.** 진입/이탈 임계를 다르게 둔다(예: 2.5 초과면 확장기,
   1.5 미만으로 내려가야 정착기). 안 그러면 매일 모드가 바뀐다.
4. 전환은 자동으로 하되 알린다 — "이제 오답이 따라잡을 만한 양이에요." 좋은
   소식이라 알릴 가치가 있다.
5. **모드는 2개에서 끝낸다.** 3개가 되는 순간 사용자도 우리도 어느 모드인지
   헷갈린다.

하루 정원(`DAILY_LIMIT_OPTIONS`)도 순수 수동이라 하루 10장 푸는 사람이 20에
머문다. **최근 7일 응시량 비례 제안 + 수동 확정**으로 바꾼다. 완전 자동은 위험하다
— 하루 쉬면 정원이 줄어드는 건 벌처럼 느껴진다.

## 결정 기록 — `keyword_title`은 개념 축이 아니다 (2026-08-05, 실측)

확장기 헤드라인을 "개념 커버리지"(전체 개념 중 손댄 비율)로 하려다 실측으로 반증.
전량 조회해서 잰 결과 — 해설 41,832건 / 문항 25,650개, `keyword_title` 커버리지 100%:

| 과목 | 문항 | 개념 | 1문항짜리 개념 | 개념당 문항 중앙값 |
|---|---|---|---|---|
| 국어 | 864 | 848 | 98.6% | 1 |
| 행정법총론 | 677 | 660 | 97.7% | 1 |
| 영어 | 672 | 669 | 99.6% | 1 |
| 한국사 | 659 | 648 | 98.5% | 1 |

**개념 수 ≈ 문항 수다.** 실제 값도 "중실축과 중공축의 전달 토크비", "양단 고정봉의
열응력" 처럼 재사용 가능한 개념이 아니라 **그 문항 한 개의 제목**이다.

정규화(공백·조사·괄호 제거)로 합쳐지는 건 전체에서 83그룹뿐이고 그마저
`단상 유도전동기 / 단상 유도 전동기` 같은 표기 차이다. **정규화로 해결될 문제가
아니다** — 로드맵 216줄의 "유사 키워드 병합 AI 배치"로도 안 된다.

### 이 측정으로 무효가 된 것

1. **개념 커버리지 헤드라인** — 분모가 848이면 "문항 848개 중 47개"와 같은 말이라
   정보가 0이다.
2. **개념 블록 편성** — "처분성 기출 12문항" 같은 학습지 묶음을 만들 수 없다.
   같은 개념의 다른 기출이 없다.
3. **개념 기반 대체 출제**(2026-08-04 "남겨둔 것" 3번) — 대체할 문항이 없다.
   `collectConceptReviewCandidates`(`review-session.ts`)는 존재하지만 후보가
   사실상 0이다.
4. AI 진단의 "취약 개념 top N"은 실제로는 "자주 틀린 문항 top N"이다. 이미 배포된
   기능이라 당장 깨지진 않지만, 개념 단위 분석으로 확대하지 말 것.

### 남겨둔 길 — `questions.unit_tag`

진짜 개념 축을 만들려면 `unit_tag`(스키마에 예약만 되고 미사용)를 채워야 한다.
**별도 과제로 분리하되, 생각보다 작다.**

견적의 핵심은 **`keyword_title`이 이미 25,650문항 전부에 붙어 있다**는 것이다.
단원 분류는 이미지를 다시 보는 작업이 아니라 "이 문항 제목이 어느 단원인가"라는
짧은 텍스트 분류다(입력이 한 줄이라 토큰이 작고, 정답·이미지가 필요 없다).
해설 배치와 같은 흐름이면 API 실비도 0이다.

| 범위 | 문항 | 견적 |
|---|---|---|
| 주요 6과목 (국어·영어·한국사·행정법총론·행정학개론·헌법) | ~3,900 | 2~3일 |
| 전 과목 | 25,650 | 1~2주 |

실제 부담은 분류 실행이 아니라 **과목별 단원 체계를 정하는 것**이다. 공시 커리큘럼이
외부에 존재하므로 창작이 아니라 정리에 가깝다.

착수 순서: ① 과목별 단원 체계 확정 → ② 해설 배치 루틴에 `unit_tag` 생성 추가
(신규 문항은 자동으로 붙어 백필 대상이 시간이 갈수록 줄어든다) → ③ 기존 문항 백필.
`unit_tag`가 생기면 개념 블록·대체 출제·진단 품질이 한꺼번에 살아난다.

**검증 주의**: 크롭과 같은 함정이 있다 — "전 문항에 태그가 붙었다"는 성공의 증거가
아니다. 과목별 단원 분포를 눈으로 볼 것(한 단원에 몰려 있으면 분류가 아니라 기본값
찍기다).

## 검토 중 — SRS를 상태에서 랭킹 항으로 (2026-08-05, 미확정)

국면 이원화 이전에 "예약(`srs_due_at`)이라는 지속 상태를 버리고 매일 랭킹으로
뽑자"를 검토했다. 근거는 대기 풀 정체·극복/예약 의미 충돌·회독과의 충돌이 전부
"상태 유지 비용"에서 나온다는 것이었다.

**국면 이원화가 이 안의 필요성을 약화시킨다.** 위 문제들은 대부분 확장기 사용자의
증상인데, 확장기에서는 애초에 간격 반복이 주력이 아니게 된다. 정착기 사용자
(유입 ≈ 처리)에게는 지금 SRS가 잘 맞고, 대기 풀도 정체하지 않는다.

→ **확장기 모드를 먼저 배포하고, 그 뒤에 정착기 사용자 데이터(`srs_reviews`)를
보고 다시 판단한다.** 지금 갈아엎으면 검증되지 않은 두 변경을 동시에 넣게 된다.

당시 정리해 둔 설계(나중에 필요하면 여기서 시작):

- 층 확장: 재확인 / 반복(`wrong_count ≥ 2`) / 최근 30일 / 꼬리 / **유지**(극복했지만
  마지막 정답 이후 경과일 ≥ f(reps, ease)) — 마지막 층이 SRS의 due를 확률로 바꾼 것.
- **시드 고정이 필수**: `rand = seededRandom(hash(userId + srsDayIndex(now)))`.
  안 하면 새로고침할 때마다 오늘의 학습지가 바뀌어 "학습지"가 아니라 뽑기가 된다.
  `buildDueQueue`가 순수·결정적이어야 하는 이유(배너 숫자 = 세션 문항)와 같은 자리.
  `pickWeightedReviewCandidates`에 이미 `opts.rand` 주입점이 있다.
- 랜덤을 넣지 말 자리: 재확인 층(망각 곡선 급경사 구간), 하루 총량.
- 최근 K일에 낸 것 감쇠(×0.4). "어제 그거 또?"를 막는 건 랜덤이 아니라 이 감쇠다.
- 측정 지표: **재탕률**(지난 7일 안에 이미 낸 문항 비율). 60% 넘으면 신선함이 무너진다.
- 재고가 적을 때(미극복 25개에 하루 20개)는 랜덤이 답이 아니다 — "유지" 층 확대나
  정원 자동 축소가 필요하다.

## 결정 기록 — "완전 극복" 마크 (2026-08-05, 미구현)

사용자가 "이 문제는 이제 확실히 안다"고 표시해 출제에서 영구히 빼는 기능.
**절반은 이미 있다** — `wrong_note_marks.deleted`가 출제·통계에서 완전히 제외하고
되돌리기 토스트(8초)까지 있다.

문제는 **프레이밍이 정반대**라는 것이다. 지금은 쓰레기통 아이콘 + "완전히
삭제할까요?" 경고라, 아무도 이걸 "내가 마스터한 문제 표시"로 읽지 않는다. 기능은
있는데 그 용도로 안 쓰이는 상태다.

→ `wrong_note_marks`에 `mastered boolean not null default false` 한 컬럼.
RLS는 이미 본인 행만 CRUD라 그대로 간다.

| 마크 | 출제 | 남은 오답 | 극복 |
|---|---|---|---|
| `mastered` (완전 극복 — 졸업) | 제외 | 제외 | **포함** |
| `deleted` (오등록·문제 오류) | 제외 | 제외 | 제외 |

- UI는 문항 카드에 체크 아이콘 하나. "다시 볼 문제"(`pinned`)의 정확한 반대편이라
  자리가 이미 있다.
- **되돌릴 수 있어야 하고 목록을 볼 수 있어야 한다.** leech 패턴을 그대로 재사용
  (설정 모달의 "접어둔 문제 N문항 · 다시 복습에 넣기"). 조용히 사라지면 사용자는
  데이터가 날아간 걸로 읽는다.
- 나중에: `srs_reps >= 3`인 문항 카드에 "이제 안 봐도 될까요?" 칩(자동 졸업 제안).
  재료는 이미 있지만 수동 마크가 먼저다.

## 결정 기록 — "극복"이라는 단어를 두 화면이 다르게 쓴다 (2026-08-05, 미구현)

복습 결과 화면이 맞힌 문항을 전부 "🎉 N문항 극복"이라 부른다(`review-solver.tsx`).
그런데 복습 큐의 절반 이상은 **이미 극복 상태**인 문항(지난번에 맞혀 간격이 붙어
예약된 것)이라, 그걸 또 맞혀도 `last_is_correct`는 true → true — **남은 오답이
1도 안 줄어든다.** 사용자는 "15문항 극복!"을 보고 오답노트에 가서 470 그대로를
본다. 반대로 복습에서 틀리면 극복이던 문항이 미극복으로 돌아가 **남은 오답이 는다.**

데이터 연동은 정상이다(복습 채점도 `recordQuestionResults(..., "review")`를 거친다).
**연동 실패가 아니라 용어 오용이다.**

→ "극복"은 **상태 전환에만** 쓴다. `recordQuestionResults`가 이미 `before`를 읽고
있어 실제로 뒤집힌 개수를 추가 조회 없이 셀 수 있다.

- 결과 화면: "정답 15/20 · **새로 극복 3** · 나머지 12문항은 복습 통과(간격이 늘어남)"
- 틀린 것도 같은 언어로: "5문항이 오답으로 되돌아갔어요 · 내일 다시 나와요".
  지금은 남은 오답이 는 이유가 어디에도 없다.

### 함께 확인할 것

- `submitReviewSession`은 `revalidatePath`를 부르지 않는데 CBT 채점
  (`papers/actions.ts`)은 `/mypage`를 무효화한다. Next 16 + `cacheComponents`에
  마이페이지가 완전 동적이라 실제로는 매번 새로 받을 가능성이 높지만, **같은
  테이블을 갱신하는 두 경로가 비대칭인 것 자체가 의심스럽다.** 실계정으로
  "복습 완료 → 오답노트" 왕복 시 숫자가 즉시 바뀌는지 확인할 것.
- `getUnresolvedCountBySubject`가 과목별로 계산하는 `due` 필드
  (`REVIEW_COOLDOWN_HOURS` 파생)는 **어디서도 렌더되지 않는다.** SRS 이전의
  "복습 lite" 잔재로, 살려두면 언젠가 실제 복습 큐와 다른 숫자를 같은 화면에 띄운다.
- **`wrong_count`의 정의가 두 곳에서 다르다.** 오답노트 화면의 "2번 틀림"
  배지·필터는 `cbt_attempt_answers` 기준(CBT 회독만)인데, 섞어풀기 A층 기준은
  `user_question_status.wrong_count`(CBT + 복습 + 섞어풀기)다. 복습에서 한 번 더
  틀리면 화면은 "1번"인데 섞어풀기는 A층으로 취급한다. 화면을
  `user_question_status` 기준으로 통일할 것(이미 `statusByRepQ`를 조회 중이라
  추가 쿼리 없음).
- **복습 오답 → 오답노트 정답의 구멍**: 복습에서 틀리면 `reps = 0`이 되는데,
  `nextSrs`의 same-day 가드는 `reps >= 1`에서만 걸린다. 그래서 복습 결과 화면에서
  **정답을 본 직후** 섞어풀기에서 같은 문항을 맞히면 `reps`가 1로 올라가고 3시간 뒤
  재확인이 사라진다. 방금 본 정답을 고른 것이지 유지력이 아니다. 가드를 건드리면
  재확인 단계가 죽으므로, **재확인 대기 중인 문항(`reps === 0 && lapses > 0 && due
  미도래`)을 섞어풀기 후보에서 빼는 쪽**이 안전하다(`review-pick.ts` + 엣지 사본).

## 공통 리스크 메모

- **중복 시험지 표시 통합(dedup)과의 상호작용**: 같은 시험지가 track만 다르게
  두 행 존재할 수 있다(AGENTS.md 참조). 오답노트·섞어풀기는 응시한 paper_id
  기준이라 당장은 안전하지만, "같은 과목 문항 모아보기"에서 병합 그룹의 두 행에
  각각 응시 기록이 있으면 같은 문항이 두 번 보일 수 있다 — Phase 1에서
  `dedup-papers.ts`의 그룹 키로 같은 처리를 해줄 것.
- **정답·해설 RLS**: 어떤 신규 화면/채점 경로도 `paper_answers`·
  `question_explanations`를 클라이언트에서 직접 읽게 하지 말 것(전부 service role
  경유 서버 조립 — 기존 `wrong-notes.ts` 패턴 유지).
- **해설봇 RLS 회귀**: `user_question_status` 등 신규 테이블/정책 작업 시
  해설봇 계정의 `question_explanations` 쓰기 권한을 깨지 않는지 확인
  (과거 스키마 변경으로 조용히 깨진 전적 — AGENTS.md).
- **측정**: 각 Phase에 최소 지표 하나씩 — 모아보기 진입률, 섞어풀기 완주율,
  진단 재요청률(다음 날 또 받는가). Phase 4 선별은 이 숫자로 한다.

## 결정 기록 — 기출 섞어풀기 (2026-09-05, 구현 완료)

오답 섞어풀기(Phase 2)는 "내가 틀린 문항"을 다시 내는 기능이라 오답이 쌓이기 전에는
쓸 수 없다. 기출 섞어풀기는 반대로 **한 과목의 기출 전체**에서 무작위로 뽑아 새 문제를
푼다. 공시생은 같은 과목을 여러 직렬(국가직+지방직, 경찰+해경)로 준비하는 일이 흔한데,
문제지 한 장씩 골라 푸는 것보다 "국어 20문항 아무거나"가 하루 학습을 시작하기 가볍다.

**새 테이블·새 화면 종류를 만들지 않았다.** `review_sessions` 에 `scope = 'mix'` 로
저장하고, 풀이·채점·"찍었어요"·복습 스케줄 반영은 오답 섞어풀기와 같은 경로
(`review-solver.tsx`, `submitReviewSessionForUser`)를 그대로 탄다. 다른 것은 후보가
어디서 오는가뿐이다(`apps/web/src/lib/mix-practice.ts`).

- **범위는 과목 하나, 시행처는 가리지 않는다.** dedup 대표로 접은 문제지 중 정답이
  등록됐고 문항 이미지가 있는 것만, voided(전항정답·복수정답)는 뺀다. 과목 단위 풀은
  `'use cache'`(1시간, `home-data` 태그)로 둔다 — 문제지 수백 장을 요청마다 훑지 않는다.
- **설정은 급수·연도·문항 수 셋.** 급수 칩(다중 선택), 연도 칩(전체/최근 3·5·10년/직접
  구간), 문항 수 10/20/40/60 + 직접 입력(5~100). 시행처·순서 옵션은 두지 않는다(UX 원칙 5).
  셋만 남긴 건 둘 다 "지금 내가 풀 문제인가"를 가르는 축이라서다 — 9급 준비생에게 7급은
  난도가 다른 시험이고, 법·제도가 바뀐 과목은 옛 기출이 현행과 어긋난다. **연도 기본값은
  전체**다(처음부터 최근 N년으로 좁혀 두면 사용자가 모르는 채 모수를 잃는다). "최근 N년"은
  오늘이 아니라 **자료의 마지막 연도** 기준이다 — 새해 기출이 아직 안 올라온 1~3월에 오늘로
  세면 최근 3년이 2년치가 된다. 급수·연도를 함께 걸었을 때 남는 문항 수는 (등급 × 연도)
  교차 표(`MixCountCell`, 100줄 남짓)를 내려보내 화면이 서버 왕복 없이 센다. 규칙 정본은
  `packages/core/src/mix-practice.ts`.
- **급수 없는 시행처는 난도 등급으로 환산해 묶는다**(`packages/core/src/exam-level-tier.ts`).
  경찰(순경)·해경·소방·계리직은 계급직이라 `exam_papers.level` 이 null 인데, 그대로 두면
  "급수 없음" 한 칸에 소방·계리직·승진시험이 뭉쳐 순경 준비생이 자기 시험을 고를 수
  없다. 공채·경채는 9급 수준, **간부후보생은 7급 수준**(공채와 과목·난도가 다르다),
  **승진시험은 어느 급수와도 대응되지 않아 "기타"** 로 둔다 — 직류(track)로 가른다.
  칩 이름은 환산된 문제지가 섞인 등급만 "9급 수준"으로 부른다: 순경 준비생에게 "9급"은
  자기 시험이 아니라는 신호라 아예 안 누른다. **DB 의 `level` 을 채워 넣지 말 것** —
  업로드 시점의 기록이고, 경찰 문제지에 "9급"을 쓰면 과목명 규칙(`subject-label.ts` 는
  "시행처 급수" 키로 갈린다)까지 조용히 어긋난다. 새 시행처는 실제 채용 요건을 확인하고
  넣되, 모르면 넣지 않는다(틀린 등급으로 묶이면 9급 세션에 7급 문항이 섞인다).
- **같은 개념이 뭉치지 않는다.** 해설 배치가 붙인 정본 개념(`question_explanations.
  concept_id`, `merged_into` 되짚음)을 후보에 실어, 세션에 개념당 하나씩 먼저 담고 정원이
  남을 때만 둘째·셋째를 허용한다(`takeSpreadByConcept`). 해설이 없는 문항은 상한을 받지
  않는다 — 커버리지가 낮은 과목에서 정원을 못 채우면 안 된다. 우선순위는 "안 푼 문항 먼저"
  가 위다(푼 문항으로 개념 겹침을 피하지 않는다).
- **안 풀어 본 문항 먼저.** `user_question_status` 에 행이 없는 문항을 먼저 뽑고, 모자랄
  때만 푼 문항으로 채운다. 그 안에서는 문제지 라운드로빈이라 40문항짜리 회차 하나가
  세션을 독점하지 않고 시행처·연도가 자연히 섞인다.
- **오답노트에는 "9월 5일 섞어풀기"로 남는다.** 과목 오답노트 "문제지별" 탭에 문제지
  카드와 같은 층위의 카드로 붙고(같은 날 두 번째부터 "(2)"), 누르면 그 세션에서 틀린
  문항을 해설·메모·다시 볼 문제 체크와 함께 본다(`/mypage/wrong-notes/[slug]/mix/[id]`).
  날짜는 KST 기준(`mixSessionTitle`).
- **틀린 문항은 과목 오답에도 합쳐진다.** 채점이 `user_question_status` 에 기록되므로
  복습 큐·미극복 수에는 원래 들어갔지만, "문항 모아보기"와 허브 과목 카드가 응시
  (`cbt_attempts`) 기준이라 거기서는 빠져 있었다. `getSubjectWrongNoteQuestions` 가
  상태에만 있는 오답을 보태고(그때 고른 답은 `review_session_items` 에서 되짚는다),
  허브는 상태 기준 집계에만 있는 과목을 뒤에 붙인다.
- **진입점은 넷.** 메뉴바 "섞어풀기"(`/mix` — 과목 고르기 허브), 과목 페이지
  (`/subjects/[slug]`) 상단 띠, 과목 오답노트 헤더 버튼, 오답노트 허브의 과목 칩 띠 —
  모두 `/subjects/[slug]/mix` 시작 화면으로 모인다. 로그인 전에도 시작 화면은 보이고,
  시작을 누르면 로그인으로 보낸다. 메뉴 항목을 둔 건 매일 여는 기능치고 두 단계
  안쪽이라 안 보였기 때문이고, 섞어풀기는 과목을 정해야 시작되므로 메뉴는 과목만 묻는
  허브로 보낸다(설정은 그다음 화면). 허브는 즐겨찾는 과목을 위로 올리고 최근 기록
  다섯 줄을 얹는다. 색인 가치는 `/subjects` 가 가져가므로 `/mix` 는 noindex.
- **채점 이력은 `source = 'mix'`.** "오답을 다시 풀어 맞힘"과 "처음 만난 기출을
  맞힘"을 한 source 로 섞으면 유지율 측정이 흐려진다. 상태 갱신 규칙은 review 와 같다.
- **앱은 아직 없다.** Edge Function(`review-create`)은 오답 후보만 만든다. 앱에 붙일
  때는 후보 수집(`getMixPool` 의 규칙)을 `_shared` 로 옮기고 `pickMixQuestions` 를
  그대로 쓰면 된다 — 채점·기록(`review-submit`, `review-history`)은 scope 를 가리지
  않아 이미 통한다.
