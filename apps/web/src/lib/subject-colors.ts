// 과목 배지 팔레트(8슬롯)와 슬롯 해시(subjectColorIndex)는 @gongmoa/core 로 단일화
// (모바일과 공유 — core/badge-classes.ts). 이 파일은 기존 import 경로를 지키는 re-export 뿐.
export { subjectColor, SUBJECT_PALETTE_CLASSES } from "@gongmoa/core";
