// 직렬 배지 색 맵 셋(테두리·선택 탭·채움)은 @gongmoa/core 의 badge-classes.ts 로
// 단일화(모바일과 공유). 이 파일은 기존 import 경로를 지키는 re-export 뿐.
export {
  examTypeColor,
  examTypeTabColor,
  examTypeFilledColor,
  EXAM_TYPE_OUTLINE_CLASSES,
  EXAM_TYPE_TAB_CLASSES,
  EXAM_TYPE_FILLED_CLASSES,
} from "@gongmoa/core";
