// 출석 이벤트 광고(/attendance-promo.png)의 크기.
//
// 그리는 쪽(lib/attendance-promo-card.tsx)과 띄우는 쪽(components/attendance-promo-slide.tsx)이
// 같은 값을 봐야 해서 따로 떼어 둔 파일이다. 그리는 쪽은 next/og 를 쓰는 서버 전용
// 모듈이라 브라우저 컴포넌트가 그걸 통째로 import 할 수 없다.
//
// 팝업은 이 비율만큼 자리를 미리 잡아 두므로(이미지가 도착하기 전에 판이 납작하다가
// 툭 늘어나지 않게), 둘이 어긋나면 그림 위아래에 빈 띠가 생긴다.
//
// 세로형. 카카오·토스류 이벤트 팝업이 쓰는 4:5 비율에 가깝게 잡되, 바닥의 잔칙·출처
// 문구를 뺀 만큼 낮췄다(예전 900 → 792). 실제로는 CSS 로 360px 안팎의 폭에 놓이므로
// 2배로 그려 고해상도 화면에서도 글자가 뭉개지지 않게 한다.
export const PROMO_SIZE = { width: 720, height: 792 };
