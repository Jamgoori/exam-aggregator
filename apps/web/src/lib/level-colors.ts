// 급수 정렬(compareLevels)과 배지 색(levelColor)은 @gongmoa/core 로 단일화(모바일과
// 공유 — core/badge-classes.ts). 이 파일은 기존 import 경로를 지키는 re-export 뿐.
export { compareLevels, LEVEL_ORDER, levelColor, LEVEL_CLASSES } from "@gongmoa/core";
